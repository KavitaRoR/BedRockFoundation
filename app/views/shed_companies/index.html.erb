<h1>Listing shed_companies</h1>

<table>
  <tr>
    <th>Company</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shed_companies.each do |shed_company| %>
  <tr>
    <td><%= shed_company.company %></td>
    <td><%= link_to 'Show', shed_company %></td>
    <td><%= link_to 'Edit', edit_shed_company_path(shed_company) %></td>
    <td><%= link_to 'Destroy', shed_company, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shed company', new_shed_company_path %>
