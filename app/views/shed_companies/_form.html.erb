<%= form_for(@shed_company) do |f| %>
  <% if @shed_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shed_company.errors.count, "error") %> prohibited this shed_company from being saved:</h2>

      <ul>
      <% @shed_company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company %><br />
    <%= f.text_field :company %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
