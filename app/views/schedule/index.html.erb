<div class="jquery_tab">

    <div class="content_block">
		<div id="filter_links">Weeks out: 
    	<a href="/schedule/?until=-2.w">prev 2w</a> | 
    	<a href="/schedule/" style="color: #f90; font-weight: bold;">TODAY - <%= (Time.now + 2.weeks).strftime("%m/%d") %></a> | 
    	<a href="/schedule/?until=2.w"><%= (Time.now + 2.weeks).strftime("%m/%d") %> - <%= (Time.now + 3.weeks).strftime("%m/%d") %></a> | 
    	<a href="/schedule/?until=3.w"><%= (Time.now + 3.weeks).strftime("%m/%d") %> - <%= (Time.now + 5.weeks).strftime("%m/%d") %></a> | 
    	<a href="/schedule/?until=5.w"><%= (Time.now + 5.weeks).strftime("%m/%d") %> - <%= (Time.now + 7.weeks).strftime("%m/%d") %></a> | 
    	<a href="/schedule/?until=7.w"><%= (Time.now + 7.weeks).strftime("%m/%d") %> - <%= (Time.now + 9.weeks).strftime("%m/%d") %></a> |
    	<a href="/schedule/?until=9.w"><%= (Time.now + 9.weeks).strftime("%m/%d") %> - <%= (Time.now + 11.weeks).strftime("%m/%d") %></a> |
    	<a href="/schedule/?until=11.w"><%= (Time.now + 11.weeks).strftime("%m/%d") %> - <%= (Time.now + 13.weeks).strftime("%m/%d") %></a> |
    	<a href="/schedule/?until=13.w"><%= (Time.now + 13.weeks).strftime("%m/%d") %> - <%= (Time.now + 15.weeks).strftime("%m/%d") %></a> |
    	<a href="/schedule/?until=15.w"><%= (Time.now + 15.weeks).strftime("%m/%d") %> - <%= (Time.now + 17.weeks).strftime("%m/%d") %></a> |
    	<a href="/schedule/?until=17.w"><%= (Time.now + 17.weeks).strftime("%m/%d") %> - <%= (Time.now + 19.weeks).strftime("%m/%d") %></a> |
    	<a href="/schedule/?until=19.w"><%= (Time.now + 19.weeks).strftime("%m/%d") %> - <%= (Time.now + 21.weeks).strftime("%m/%d") %></a> |
    	<a href="/schedule/?until=21.w"><%= (Time.now + 21.weeks).strftime("%m/%d") %> - <%= (Time.now + 23.weeks).strftime("%m/%d") %></a> |
    	<a href="/schedule/?until=23.w"><%= (Time.now + 23.weeks).strftime("%m/%d") %> - <%= (Time.now + 25.weeks).strftime("%m/%d") %></a> |
    	<a href="/schedule/?until=25.w"><%= (Time.now + 25.weeks).strftime("%m/%d") %> - <%= (Time.now + 27.weeks).strftime("%m/%d") %></a> |
    	<a href="/schedule/?until=27.w"><%= (Time.now + 27.weeks).strftime("%m/%d") %> - <%= (Time.now + 29.weeks).strftime("%m/%d") %></a> |
    	<a href="/schedule/?until=29.w"><%= (Time.now + 29.weeks).strftime("%m/%d") %> - <%= (Time.now + 31.weeks).strftime("%m/%d") %></a> 
    	</div>
	

			<div id="today-marker"></div>
			<% ((@query_start_date)..(@query_start_date + 2.weeks)).each do |day| %>
				<%= render :partial => "day_table", :locals => {:day => day} %>
			<% end %>
		<% u = params[:until].split(".").first.to_i rescue 0 %>
		<div style="margin-bottom: 40px;">
			<a href="/schedule/?until=<%= u + 2 %>.w" class="button huge blue-gradient icon-right-fat">Show me the next 2 weeks </a>  </div>
	    
    </div>
</div>


<%= render :partial => "queued_contracts" %>

<%= content_for :js_bottom do %>
	<%= render :partial => 'persistence' %>
<% end %>

<script type="text/javascript">
	$(document).ready(function() {
		$("#show-previous-button").click(function() {
			$("#show-previous-button").hide();
			$("#hide-previous-button").show();
			$(".previous-calendar").slideDown('slow', function() {
				location.hash = "#today-marker";
			});			
		})
		
		$("#hide-previous-button").click(function() {
			$(".previous-calendar").slideUp();
			$("#show-previous-button").show();
			$("#hide-previous-button").hide();
		})
	})
</script>