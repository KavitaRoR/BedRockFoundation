<%= form_for(@content_datum) do |f| %>
  <% if @content_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content_datum.errors.count, "error") %> prohibited this content_datum from being saved:</h2>

      <ul>
      <% @content_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %><br />
    <%= f.text_field :key %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_area :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
