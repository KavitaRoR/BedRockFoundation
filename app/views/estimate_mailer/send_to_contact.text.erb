Dear <%= @estimate.job.contact.first_name %> <%= @estimate.job.contact.last_name %>,
	<% vars = YAML::load(@estimate.flashvars).with_indifferent_access %>
	I've created completed an estimate for a <%= vars[:job_width] %>' x <%= vars[:job_length] %>' <%= vars[:foundation_kind] %> Foundation.
	To access the estimate, you will need to have the Adobe's Flash Player installed.  Please feel free to call me with any questions, comments, or concerns.
	
	To view your estimate follow this url at http://localhost:3000/estimates/client_estimate/<%= @estimate.token %> to view it. 

Yours truly,

Eli Stoltzfus
