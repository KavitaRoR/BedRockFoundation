<h1>Listing job_additions</h1>

<table>
  <tr>
    <th>Addition price in cents</th>
    <th>Addition description</th>
    <th>Job</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @job_additions.each do |job_addition| %>
  <tr>
    <td><%= job_addition.addition_price_in_cents %></td>
    <td><%= job_addition.addition_description %></td>
    <td><%= job_addition.job_id %></td>
    <td><%= link_to 'Show', job_addition %></td>
    <td><%= link_to 'Edit', edit_job_addition_path(job_addition) %></td>
    <td><%= link_to 'Destroy', job_addition, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Job addition', new_job_addition_path %>
