<%= form_for(@job_addition) do |f| %>
  <% if @job_addition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_addition.errors.count, "error") %> prohibited this job_addition from being saved:</h2>

      <ul>
      <% @job_addition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :addition_price_in_cents %><br />
    <%= f.text_field :addition_price_in_cents %>
  </div>
  <div class="field">
    <%= f.label :addition_description %><br />
    <%= f.text_area :addition_description %>
  </div>
	<%= f.hidden_field :job_id, :value => @job_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
